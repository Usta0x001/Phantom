<tools>
  <tool name="record_finding">
    <description>Record an important discovery into the persistent findings ledger.

The findings ledger is NEVER compressed or summarised — it survives across memory compression cycles, so anything recorded here is guaranteed to remain available for the rest of the scan.

Use this tool to save:
- Confirmed vulnerability details (type, location, evidence)
- Discovered endpoints and API routes
- Technology versions and stack info
- Credentials, tokens, secrets found
- Important observations about target behaviour
- Dead-end notes (to avoid re-testing)</description>
    <parameters>
      <parameter name="finding" type="string" required="true">
        <description>A concise one-line description of the discovery. Example: "SQLi confirmed at POST /rest/user/login param=email"</description>
      </parameter>
      <parameter name="category" type="string" required="false">
        <description>Category tag: "vuln", "endpoint", "tech", "credential", "dead-end", or "general" (default: "general")</description>
      </parameter>
    </parameters>
    <returns type="Dict[str, Any]">
      <description>Confirmation with current ledger size</description>
    </returns>
  </tool>

  <tool name="get_findings_ledger">
    <description>Retrieve all recorded findings from the persistent ledger.

Use this to review what has been discovered so far and avoid duplicate testing.</description>
    <returns type="Dict[str, Any]">
      <description>Complete list of recorded findings with count</description>
    </returns>
  </tool>
</tools>
